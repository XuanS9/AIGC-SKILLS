# video-script-studio：提示词内容边界修复

日期：2026-09-24。范围：仅修改 `video-script-studio/SKILL.md` 的提示词输出规则；原有测试保留，追加 3 条针对性用例。基准提交：`9a20837`。分支：`auto-optimize/20260924-1030`。

## 问题与修改

用户反馈：提示词中的“不提前出现某道具/角色”会把本来无需描述的对象带进生成上下文。旧版要求检查道具时序，又要求填写约束/排除栏目，但未明确内部检查与模型输入之间的边界。

本轮针对 dim8 的输出污染问题：

- 先按每次独立生成的范围建立内部出场/状态表；项目资产总表不等于当前出场表。
- 当前范围外的实体、状态、别名和标签不写入任何可复制栏目，也不搬到负面栏。以已有事实正向表述，不编造替代动作。
- 同一片段中较晚登场的对象保留，并在对应时点写入画、递交、持有及变化；保留必要画外声音。
- 结尾只交接当前结束状态；无新增约束时空栏/省略可选字段，防止为填栏目补清单。
- 参考图本身夹带对象时，在块外安排清理或更换，素材未处理则只给条件性样本。
- 保留无文字/水印、无头取景和静音等合法输出规格，不做全局删否定词。

## 测试设计与执行

`video-script-studio/test-prompts.json` 中原 id 1–2 保留，本轮新增 id 3–5：

1. 只生成河岸徒步第一镜，后续才出现父亲、折刀和落水状态；检查全部复制字段是否泄漏后续内容。
2. 同一个 8 秒片段内，第 3 秒父亲持折刀入画、第 5 秒开始递交；检查登场与转移顺序没有因净化而丢失。
3. 基础角色六行组板，旧参考夹带标枪/背包且后续有受伤状态；检查复制块、六行格式和素材处理说明。

每例以旧入口、新入口、无技能三种上下文各执行一次独立文本生成。使用本机已有 `pi`、已配置 `A6 GPT / gpt-6-astra`，关闭会话、技能自动加载、扩展、工作区上下文和工具。旧/新两组附完全相同的相关参考文本；无技能组只收到执行说明与用户请求。未附的参考没有假装读取，测试不是完整工具工作流。完整原始输出及退出状态见 `darwin-prompt-boundary-evidence.json`。

## 验证范围

- 文字层面的污染复现、时序语义核对，以及提取实际角色代码块运行入口现有六行校验器。
- 仓库 `verify_merge.py`、入口链接、`git diff --check`、文件大小上限。
- 三位独立评审各在同一调用中阅读旧/新入口和三组输出，按九维 rubric 作 paired 比较；第二位交换两版展示顺序以减轻位置偏差。此处“独立”指隔离模型调用，不代表不同模型。投票作为 keep/revert 依据，不用绝对分数差值。
- 不调用视频/图像生成，不能据此保证视觉效果；每场景每版本仅一个样本，不能估计稳定通过率。

## 已观察到的结果

- id 3：旧版与无技能复制块均含父亲、折刀、湿发、湿裙和落水；新版复制块这些范围外词项为零，同时保留沿河岸徒步和双手摆动。
- id 4：新版保留 3 秒入画、5 秒起递交、右手接稳后垂手持有；未把晚登场人物/道具删除。
- id 5：旧/新版角色复制块均通过现有六行检查，均未包含标枪、背包、受伤和破损；因此该例用于检查格式回归，不宣称所有场景都有提升。
- 已知局限：新版仍有少量素材可用性说明混入视频代码块；id 5 块外说明旧图未提供，但没有明确重述绑定前应清理/更换旧图。提示词规则已规定处理路径，单次文本执行仍可能遗漏；不把它计作完整通过。

## 独立评审与最终状态

最终定稿在片段内晚登场规则中补写「不用否定时序句，改写为对象在节点前的实际持有/场外状态」，去掉 id 4 输出里遗留的“不提前交接”式否定措辞。以此定稿重跑 id 3–5 文本生成（`darwin-prompt-boundary-final-evidence.json`）：

- id 3：可复制块范围外词项 0 处，保留徒步与摆臂。
- id 4：父亲 3 秒入画、5 秒递刀、右手接稳的正向时序完整保留。
- id 5：六行组板校验通过，标枪/背包/伤势/破损未泄漏。

三位独立评审在同一调用内比较基准 `9a20837` 与定稿，附旧/新两组文本证据，评审 2 交换展示顺序减轻位置偏差。结果 **3 better / 0 worse / 0 tie，margin=clear**（`darwin-prompt-boundary-paired.json`）。按 paired 多数决 **keep**。

共同指出的局限：仅文本层验证，未生成视频/图像，也未运行真实素材绑定；每场景单样本，不估稳定通过率；参考图夹带对象时的“先裁切/清理/更换再绑定”说明在实际输出中仍可能遗漏，规则已写但单次执行不保证。这些计入使用时的人审项，不宣称已消除。成果卡片见 `darwin-prompt-boundary-card.png`。
